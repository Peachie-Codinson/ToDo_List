To-Do List app.  Allows users to create, edit, delete, or mark tasks as done. 

App was created with a React frontend and Django Backend. This app is made for local deployment on the browser of choice. 

Run docker-compose up --build in the project directory, access [https](http://localhost:3000) after frontend deployment to view the app.
